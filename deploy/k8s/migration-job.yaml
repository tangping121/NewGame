apiVersion: batch/v1
kind: Job
metadata:
  name: bastion-schema-migrate
  namespace: bastion
spec:
  backoffLimit: 4
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app: bastion-schema-migrate
    spec:
      restartPolicy: Never
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: migrate
          image: bastion/migrate:0.2.0
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: NG_POSTGRES
              valueFrom:
                secretKeyRef:
                  name: bastion-secrets
                  key: postgres-central
            - name: NG_POSTGRES_SHARDS
              valueFrom:
                secretKeyRef:
                  name: bastion-secrets
                  key: postgres-shards
            - name: NG_SHARD_COUNT
              value: "50"
          resources:
            requests: { cpu: "50m", memory: "32Mi" }
            limits: { cpu: "250m", memory: "128Mi" }
